# =============================================================================
# Jetson Edge AI Point - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.template .env
# =============================================================================

# -----------------------------------------------------------------------------
# Network Configuration (macvlan)
# -----------------------------------------------------------------------------
# Host network interface (check with: ip link show)
HOST_INTERFACE=eno1

# Subnet and gateway of your LAN
NETWORK_SUBNET=192.168.0.0/24
NETWORK_GATEWAY=192.168.0.1

# IP range for macvlan containers (must be within subnet, excluded from DHCP)
MACVLAN_IP_RANGE=192.168.0.232/29

# Static IPs for services (must be within MACVLAN_IP_RANGE)
OLLAMA_IP=192.168.0.233
WHISPER_IP=192.168.0.234
# Optional dedicated IP for Ollama Gradio UI
OLLAMA_UI_IP=192.168.0.235

# -----------------------------------------------------------------------------
# Ollama / LLM Configuration
# -----------------------------------------------------------------------------
# Model to pull on startup
OLLAMA_MODEL=PetrosStav/gemma3-tools:4b

# -----------------------------------------------------------------------------
# Google Search Tool Credentials (for Gemma3 tool-calling)
# -----------------------------------------------------------------------------
GOOGLE_API_KEY=AIzaSyDeIeEPZecyp9y6Ug-vgLsmR_RHerwcNTU
GOOGLE_CX_ID=b1adfd66355dc4ffb

# -----------------------------------------------------------------------------
# Whisper Configuration
# -----------------------------------------------------------------------------
# Whisper model size: tiny, base, small, medium, large
WHISPER_MODEL=base

# -----------------------------------------------------------------------------
# Ollama Chat UI (Gradio + Postgres Memory)
# -----------------------------------------------------------------------------
OLLAMA_UI_PORT=7861

# Postgres connection parameters (memory store)
POSTGRES_HOST=192.168.0.134
POSTGRES_PORT=5432
POSTGRES_DB=ollama_memory
POSTGRES_USER=ollama
POSTGRES_PASSWORD=changeme

# -----------------------------------------------------------------------------
# Watchtower Configuration
# -----------------------------------------------------------------------------
# Check interval in seconds (default: 86400 = 24 hours)
WATCHTOWER_POLL_INTERVAL=86400
